# Docker 环境配置模板
# 将此文件复制为 .env.docker 并填入你自己的值。

# --- 后端服务 ---
# 运行 `docker-compose run --rm backend python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'` 生成
SECRET_KEY=

# 数据库连接 (与 docker-compose.yml 中的 `db` 服务匹配)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=saleor
DB_USER=saleor
DB_PASSWORD=saleor
DB_HOST=db
DB_PORT=5432

# --- 前端服务 ---
# 后端 API 地址
NEXT_PUBLIC_API_URL=http://localhost:8000/graphql/
REACT_APP_API_URL=http://localhost:8000/graphql/
